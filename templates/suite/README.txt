RiM-TiR suite $version ($target_id)

Install:
  sudo ./install.sh

Uninstall:
  sudo ./uninstall.sh

Package checksums are stored in checksums.sha256.
Component versions and source artifacts are stored in manifest.json.
