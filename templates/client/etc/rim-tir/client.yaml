http:
  addr: 0.0.0.0:8080

web:
  path: /usr/share/rim-tir-client/web

database:
  sqlite_path: /var/lib/rim-tir/client/app.db

paths:
  data: /var/lib/rim-tir/client
  logs: /var/log/rim-tir/client

mqtt:
  broker_url: tcp://127.0.0.1:1883
  command_topic: command
  audit_topic: audit

auth:
  bootstrap_admin:
    login: admin
    password: admin
